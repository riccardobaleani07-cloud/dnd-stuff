package Enums;
public enum TravelRule {
    FREE,            // Anyone can enter
    RESTRICTED,      // Requires conditions
    ONE_WAY,         // Entry only, no exit
    INVITATION_ONLY, // Must be chosen
    FORBIDDEN        // Absolute denial
}
